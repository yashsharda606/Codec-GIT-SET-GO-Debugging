include <stdio.h>

int main() {
    int x, y, sum;
    printf("Enter two numbers: ");
    scanf("%d %d", &x, &y);  
    sum = x + y;
    printf("Sum: %f", sum);  
    printf("hritika")
    return 0;
}
